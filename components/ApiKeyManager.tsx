import React from 'react';

// This component is deprecated and no longer used.
// It has been replaced by the ApiKeySettings component for external API key management.
// Keeping this file as a placeholder to ensure clean builds if referenced by legacy imports.
const ApiKeyManager: React.FC<{ onReady: () => void }> = ({ onReady }) => {
  return null;
};

export default ApiKeyManager;